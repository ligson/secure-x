# Changelog

All notable changes to this project will be documented in this file.

## [Unreleased]

## [v1.0.33] - 2026-06-04

### Fixed

- 通话保持亮屏改为全局活跃通话周期校验：等待接听、接入 LiveKit、通话中和前台恢复后都会重新确认 wakelock 状态，避免移动端长时间通话或入房失败重试期间被系统锁屏。
- 通话页局部释放 wakelock 时会先检查全局活跃通话状态，避免页面生命周期和全局通话保活互相覆盖导致屏幕保持策略被提前关闭。

## [v1.0.32] - 2026-06-04

### Fixed

- 修复 LiveKit 入房失败后自动补偿重试可能反复创建原生连接的问题：失败路径会主动释放 room/listener，自动重试最多执行一次，避免移动端长时间卡顿或系统提示应用无响应。
- 音视频通话会按后端 `turnMode` 选择 ICE 策略；只有明确 `turn_tls_443` 时才强制 relay，其它部署模式允许直连与 TURN 候选共同参与，避免私有部署 TURN/TLS 证书链兼容性问题导致完全无法入房。

## [v1.0.31] - 2026-06-03

### Added

- App 会按全局活跃通话状态保持屏幕常亮，避免等待接听、接入 LiveKit 或通话中被系统自动锁屏；通话结束后恢复系统默认锁屏策略。
- 音视频通话页头像优先显示用户上传的图片头像；左上角画中画按钮改为应用内可拖动通话浮窗，点浮窗可恢复全屏，浮窗外继续操作聊天界面，避免误退出通话页导致挂断。
- 新增 `deploy/docker-compose.yml` 私有部署模板，支持用 Docker Compose 同时部署 Secure X 后端和 LiveKit，并提供后端、LiveKit、nginx 与 `.env` 示例配置。

### Documentation

- 完善后端部署文档，补充 Docker Compose 部署流程、LiveKit 端口规划、同域名 `/livekit/` 反代、TURN/UDP 模式和 relay 端口一致性要求。

### Fixed

- 后端 Docker 镜像默认仓库名统一为 `ligson/securex-be`，私有部署 Compose 使用包含 `linux/amd64` 与 `linux/arm64` 的发版版本 tag，避免继续使用临时 dev 镜像标签。
- 修复音视频通话在双方同时呼叫或重复接听信令乱序到达时，旧的 LiveKit 入房异步任务可能污染当前通话状态的问题；LiveKit 入房现在绑定当前 `callId` 和入房 generation，切换通话或挂断会失效旧入房任务。
- 发起、接听或重试音视频通话前会刷新后端实时配置，避免客户端继续使用已经废弃或没有 DNS 解析的 LiveKit 信令域名。

## [v1.0.30] - 2026-06-02

### Added

- 新增后端 Dockerfile，支持把 `securex-be` 构建为容器镜像，并默认通过 `/app/config.yaml`、`/app/data`、`/app/logs` 运行。
- 新增 `scripts/build-image.sh`，使用 Docker buildx 构建并推送 `linux/amd64`、`linux/arm64` 多架构后端镜像，默认镜像名为 `ligson/secure-x`。
- 补充后端 Docker 镜像构建、推送与容器部署说明。

### Fixed

- 修复音视频通话发起方收到接听信令后可能没有进入 LiveKit 房间的问题：接听后会确认入房进度，必要时自动补偿重试，并上报非敏感 `join-skipped` / `join-waiting` 诊断事件，避免接听方一直停留在“等待对方进入通话”。
- 新客户端之间的 LiveKit 语音/视频通话会通过已加密通话信令协商媒体帧 E2EE 密钥，接听方明确确认后才启用 LiveKit 帧级端到端加密；旧客户端未确认时保持兼容路径。
- 优化通话诊断 ID 展示为首尾组合，避免微秒时间戳生成的多个 `callId` 因前缀相同而在日志中难以区分。

## [v1.0.29] - 2026-06-02

### Added

- 文件模块支持从文件右侧菜单分享给好友或群聊：分享会追加密文文件下载授权，并发送一条端到端加密的聊天文件引用，分享完成后会打开对应会话。
- 聊天窗口普通文件和视频发送改为复用加密文件分片上传链路，聊天消息只保存端到端加密后的文件引用，避免大文件进入聊天消息 payload 或本地聊天缓存。
- 后端文件下载支持 owner 追加授权用户，下载时同时校验文件 owner、授权列表和好友/实时通讯权限；非授权用户即使知道文件 ID 也不能下载密文文件。

### Documentation

- 新增后端部署指南，覆盖 Release 包部署、YAML 配置、HTTPS 反向代理、systemd 托管、备份升级、LiveKit 音视频部署与常见排障。
- 新增加密文件分享文档，说明聊天大文件引用、文件模块分享、密文下载授权、客户端分片加密和续传边界。

### Fixed

- 修正音视频通话状态：通话计时从远端真正进入 LiveKit 房间后开始；远端已进入后离开房间会按对方挂断处理并直接结束通话页，避免误显示“等待对方进入通话”。
- 增加双方同时呼叫的通话仲裁：同一好友出现两个并发 `callId` 时按稳定规则合并到一个通话，并兼容胜出/拒绝信令乱序到达，避免双方都卡在等待接听或误关闭通话页。
- 完善音视频通话稳定性：增加全局活跃通话状态、忙线拒绝、前后台恢复重试、LiveKit 断线手动重试、挂断信令发送顺序修复、通话阶段诊断和后端非敏感通话事件日志，诊断日志不记录 token、密钥、密文、真实部署地址或媒体内容。
- 优化音视频通话性能：通话阶段事件上报去重、LiveKit 重新接入退避节流、release 模式降级详细媒体快照日志、音频通话跳过视频 track 查询，并将通话计时改为局部刷新，减少长通话下的页面重建和额外网络请求。

## [v1.0.28] - 2026-05-30

### Fixed

- 修复实时通话加密信令在单侧重连后可能卡在旧会话密钥上的问题：收到重新连接请求时会回发当前公钥，收到新公钥时会重新派生端到端加密会话，降低接听信令发送失败概率。
- 增加通话信令与 LiveKit 入房阶段的客户端诊断日志，覆盖实时通道未连接、加密会话未就绪、信令提交/解密、凭证为空、准备入房和入房完成等关键节点，并避免记录 token、密文和私有部署信息。

### Verified

- `securex-app`: `flutter analyze`
- `securex-app`: `flutter test`

## [v1.0.27] - 2026-05-30

### Fixed

- 为 GitHub Release workflow 的 artifact 上传和下载步骤增加重试，并允许重试时覆盖半创建的 artifact，降低 GitHub artifact 服务 DNS 或网络瞬断导致 Release 失败的概率。

## [v1.0.26] - 2026-05-30

### Added

- 增加音视频通话排障日志：记录 LiveKit token 签发/拒绝原因、实时通话信令收到/转发/缓存/补投递阶段和短 ID 诊断信息，避免记录 token、密钥、密文 payload 或私有部署地址。

## [v1.0.25] - 2026-05-30

### Fixed

- 修复通话邀请、接听、挂断信令发送前反复强制重连实时 WebSocket，导致端到端加密信令会话被主动打断并提示“接听信令发送失败”的问题。
- 放宽实时加密信令会话等待时间，降低移动网络和代理链路抖动下接听信令误判失败的概率。

### Changed

- 私有 LiveKit 部署已调整为对客户端发布 TURN/TLS `443` 候选，匹配公网 SNI 分流入口，减少移动网络阻断非标准 TURN 端口导致的入房失败。

### Verified

- `securex-app`: `dart analyze securex-app`
- `securex-app`: `flutter test`

## [v1.0.24] - 2026-05-30

### Fixed

- 修复语音/视频通话接听信令在对方 WebSocket 短暂重连时被直接丢弃的问题：服务端对关键通话信令做短 TTL 内存补投递，客户端对邀请、接听、拒绝、挂断信令增加重试，接听信令未发送成功前不进入 LiveKit 房间。
- 修复视频通话远端视频未订阅成功时主窗口回退显示本地画面，导致主窗口和小窗都像是自己的视频的问题；默认主窗口只显示远端视频，本地视频保留在小窗。
- LiveKit 客户端连接强制使用 TURN relay 候选，匹配当前只暴露 TURN/TLS 的私有部署模式，降低 ICE 选到不可达候选后断开的概率。

### Verified

- `securex-be`: `go test ./...`
- `securex-app`: `dart analyze securex-app`
- `securex-app`: `flutter test`

## [v1.0.23] - 2026-05-30

### Documentation

- 补充私有部署信息不得写入 tracked 文件的协作规则，并将具体服务器、路径和端口信息改为只允许记录在本机忽略文件中。
- 将文档中的个人本机绝对路径改为仓库相对路径，并补充团队协作场景下禁止提交个人路径和敏感信息的规则。
- 补充自托管 LiveKit 的 TURN/TLS 部署说明：当公网 `443/TCP` 同时承载 HTTPS 与 TURN/TLS 时，必须使用独立 TURN 域名和四层 SNI 分流，避免客户端收到不可用的 HTTP 反代入口作为 TURN/TLS 端点。

### Verified

- 未运行代码测试；本版本仅包含文档与部署约束更新。

## [v1.0.22] - 2026-05-29

### Fixed

- 修复通话页语音通话时长在顶部和中间重复显示的问题。
- 通话控制信令固定走服务端 WebSocket 加密中继，并在发送前强制刷新实时连接，避免旧 WebRTC DataChannel 状态导致接听、挂断等信令丢失。
- LiveKit 通话接入、取凭证、开启麦克风和摄像头增加超时保护与更明确的中文断开原因，降低移动端长时间卡在“正在获取通话凭证”或系统判定无响应的概率。
- LiveKit 入房身份改为用户设备级身份，避免同一账号多设备在线时被 LiveKit 判定为重复身份并断开通话。

### Verified

- `securex-be`: `go test ./...`
- `securex-app`: `flutter analyze`
- `securex-app`: `flutter test`

## [v1.0.21] - 2026-05-29

### Fixed

- 通话接通后显式唤起 LiveKit 远端音频播放，降低移动端接通后无声音的概率。
- 优化语音/视频通话界面：接通后显示通话时长；视频通话默认远端画面为主画面、本地画面为右上角小窗，小窗点击可切换主次画面。
- 应用启动时显式初始化 LiveKit 底层 WebRTC 插件；通话页增加更多 LiveKit 轨道发布/订阅事件监听和中文诊断日志，远端或本地音视频轨道变化时立即刷新画面。

### Verified

- `securex-be`: `go test ./...`
- `securex-app`: `flutter analyze`
- `securex-app`: `flutter test`

## [v1.0.20] - 2026-05-29

### Fixed

- 修复 `v1.0.19` 在 GitHub iOS runner 上因为 `device_info_plus 12.4.0` 调用 `NSProcessInfo.isiOSAppOnVision` 导致 Xcode 16.4 编译失败的问题，固定到兼容当前 runner SDK 的 `device_info_plus 12.3.0`。

### Verified

- `securex-be`: `go test ./...`
- `securex-app`: `flutter analyze`
- `securex-app`: `flutter build macos --release --build-name 1.0.20 --build-number 10020`
- `securex-app`: `flutter build ios --release --no-codesign --build-name 1.0.20 --build-number 10020` 已通过 CocoaPods 依赖解析；本机因未安装 iOS 26.5 SDK 停在 Xcode 目标平台检查

## [v1.0.19] - 2026-05-29

### Fixed

- 修复 `v1.0.18` 在 GitHub macOS/iOS runner 上因为 `connectivity_plus 7.1.1` 调用 `NWPath.isUltraConstrained` 导致 Swift 编译失败的问题，临时固定到 `connectivity_plus 6.1.5`，同时保留 `livekit_client 2.7.0` 与 `flutter_webrtc 1.4.0` 的 WebRTC SDK 兼容组合。

### Verified

- `securex-be`: `go test ./...`
- `securex-app`: `flutter analyze`
- `securex-app`: `flutter build macos --release --build-name 1.0.19 --build-number 10019`
- `securex-app`: `flutter build ios --release --no-codesign --build-name 1.0.19 --build-number 10019` 已通过 CocoaPods 依赖解析；本机因未安装 iOS 26.5 SDK 停在 Xcode 目标平台检查

## [v1.0.18] - 2026-05-29

### Fixed

- 修复 macOS/iOS Release 构建失败：升级 `livekit_client` 并将 `flutter_webrtc` 锁定到兼容版本，使 CocoaPods 解析到一致的 `WebRTC-SDK 144.7559.01`。
- 修复 iOS Podfile 未显式声明平台版本导致的 CocoaPods 警告，明确使用 `platform :ios, '13.0'`。

### Updated

- GitHub Actions 官方 action 升级到 Node 24 版本，避免继续出现 `Node.js 20 actions are deprecated` 警告。
- GitHub Actions Flutter action 固定到当前小版本，降低发版构建链路的不确定性。

### Verified

- `securex-be`: `go test ./...`
- `securex-app`: `flutter analyze`
- `securex-app`: `flutter build macos --release --build-name 1.0.18 --build-number 10018`
- `securex-app`: `flutter build ios --release --no-codesign --build-name 1.0.18 --build-number 10018` 已通过 CocoaPods 依赖解析；本机因未安装 iOS 26.5 SDK 停在 Xcode 目标平台检查，GitHub macOS runner 会提供对应 SDK

## [v1.0.17] - 2026-05-29

### Fixed

- 修复聊天图片、语音、视频和文件附件使用大 base64 消息体导致的待发送、实时推送失败和聊天窗卡顿：客户端现在先本地加密附件，再上传为服务端不可解密的聊天附件密文对象，聊天消息只携带轻量引用和解密参数。
- 优化聊天图片渲染：客户端会缓存已下载并解密的附件 bytes，避免聊天窗口滚动或刷新时反复 base64 解码造成闪动和卡顿。
- 修复语音/视频通话邀请信令可能因端到端会话密钥尚未协商完成而直接失败的问题；呼叫信令发送和接收都会等待安全会话可用后再处理。
- 调整语音/视频通话页面布局，避免手机端状态文字被底部控制按钮遮挡或挤压。

### Updated

- 后端新增聊天附件密文存储接口，下载时会校验所有者或允许接收用户，服务端仍不能解密附件内容。
- 语音/视频通话媒体链路改为自托管 `LiveKit`：后端通过 YAML 配置 LiveKit 地址和 API 密钥，并在校验好友实时权限后签发短期房间 token；客户端通过实时配置接口获取 RTC 服务信息，不再硬编码通话端口或自行交换 WebRTC offer/answer/candidate。
- 实时服务配置支持返回可选 `rtc` 配置，当前用于 LiveKit 自托管通话；图文聊天仍保持 `WebSocket + 应用层端到端加密` 主链路。

### Verified

- `securex-be`: `go test ./...`
- `securex-app`: `flutter analyze`

## [v1.0.16] - 2026-05-28

### Added

- 聊天输入区新增“发送图片”和“发送文件”：附件会作为端到端加密消息体发送，服务端仍只保存不可解密密文信封；图片会在客户端压缩后发送，单个聊天附件暂限制为 2MB，避免撑爆实时队列和聊天归档。
- 聊天输入区调整为类微信交互：默认显示表情按钮和加号按钮，输入框有文本时加号自动切换为发送按钮；表情按钮会展开 emoji 面板，表情面板和加号工具面板互斥显示。
- 聊天输入区新增语音消息：点击麦克风开始录音，再次点击停止并发送；语音作为端到端加密附件走现有聊天密文通道，服务端无法解密内容，聊天气泡支持点击播放。
- 聊天加号面板的“相册”现在支持选择本地图片和视频发送：图片仍会客户端压缩后加密发送，短视频作为 `video` 加密附件发送，暂限制 20MB。
- 聊天加号面板新增“视频通话”入口：点击后按微信样式弹出“视频通话 / 语音通话 / 取消”，支持来电接听/拒绝、发起方取消、挂断、本地麦克风/扬声器/摄像头开关，并通过端到端加密呼叫信令交换 WebRTC offer/answer/candidate 来建立远端音视频轨道。

## [v1.0.15] - 2026-05-28

### Fixed

- 加固聊天设备身份稳定性：客户端现在会把 `deviceId + seedBase64` 一起写入本地加密兜底，并同步一份只有客户端能解密的服务端设备恢复包；安全存储偶发失效、卸载重装后只要能登录并解锁，就优先恢复同一个 Secure X 设备身份，避免同一设备反复注册新 `deviceId`。
- 新增聊天设备注册防爆保护：服务端限制单用户每分钟新增设备数，继续保留最近设备裁剪和活跃设备投递上限，避免异常客户端再次把设备目录打到数万条。
- 修复聊天设备历史记录膨胀导致单聊消息像“丢失”的问题：后端查询目标用户设备时只返回近期活跃设备并设置数量上限，同时补充聊天设备与待拉取密文队列索引，并自动裁剪过量历史设备注册，避免旧客户端反复生成设备后把单次发送放大成数万次加密投递。
- 强化聊天消息“不丢且 10 秒内可感知”的投递链路：发送端先本地落盘再发网络请求，单聊/群聊发送超过 9 秒会转入后台重试，避免界面长时间卡在发送流程。
- 优化待拉取密文队列：`chat-pending` 现在会按发送者过滤拉取，避免新消息被其他历史积压消息挡住；客户端处理满页待收消息后会继续异步补拉下一页。
- 优化聊天批量入队：服务端对过期或无效目标设备改为逐条跳过并记录，合法设备继续批量保存密文信封，避免一个坏设备导致整批消息无法入队。
- 优化聊天送达确认：接收端成功解密并写入本地会话后立即确认服务端队列，送达回执改为后台限时发送，避免回执链路拖慢或阻塞消息确认。
- 降低实时通知排队延迟：服务端 websocket 发送缓冲扩大，客户端待收消息兜底轮询调整为 5 秒一次，减少弱网或代理抖动时超过 10 秒才看到消息的概率。
- 重构个人信息页：顶部集中展示用户名、邮箱、当前服务等不可修改信息，可修改项改为列表入口，头像和昵称分别进入独立子页面修改，交互更接近系统设置。
- 新增本地头像上传：个人头像除预设头像外，现在可以选择本地图像，客户端会压缩到适合头像展示的尺寸，服务端校验 jpg/png/webp 与 1MB 大小上限后保存并返回头像地址；好友、聊天和个人资料会优先展示上传头像。
- 修复 SQLite 并发锁导致老用户登录失败的问题：后端启用 WAL、busy timeout，并将 SQLite 连接串行化，降低聊天设备高频写入把登录、分类新增等请求锁住或排队超时的概率；登录与当前用户接口也会区分数据库异常和账号密码错误，避免把服务端锁库误提示为“用户名或密码不正确”。
- 优化登录会话失效处理：后端记录 401 鉴权失败的最小诊断日志，客户端收到 401 时会清理本地会话并回到登录页，避免旧 token 继续停留在业务页面造成“后端超时”的误判。
- 降低实时聊天重连风暴对后端的压力：同一聊天设备短时间内不再重复注册设备公钥，避免弱网或恢复连接时产生大量 `chat/devices/current` 写请求。

### Updated

- 设置的安全设置下新增“设备管理”，可以查看当前账号的聊天设备、最近在线时间，并手动移除旧设备；当前正在使用的设备默认不能删除。
- 密码库首页的树形分类筛选改为单选下拉框，保留“分类管理”入口；选择父分类时仍会包含其子分类，避免分类过多时首页被树形列表撑得过长。

## [v1.0.14] - 2026-05-27

### Updated

- 新增预置头像能力：个人资料现在可以选择并保存预置头像；好友列表、好友详情、聊天会话、群成员列表与群资料页统一展示对应头像，避免继续使用字母占位块
- 新增群头像管理：群聊创建时会带默认群头像，群管理可在群信息页修改群头像；群头像会随群快照、历史同步与实时群控制消息一起加密同步到成员侧，不额外暴露服务端可读明文
- 调整头像刷新策略：个人头像和群头像不再要求实时更新；打开好友信息页或群信息页时会静默刷新一次资料，其他页面可在后续正常刷新时更新，减少不必要的实时同步耦合
- 新增个人昵称能力：后端用户资料增加 `nickname` 字段，客户端设置页可单独修改昵称；昵称仅用于展示，登录仍使用用户名或邮箱
- 新增好友备注昵称能力：当前账号可为好友设置或清除备注昵称；备注内容会先由客户端使用保险库密钥加密，再以密文形式保存到服务端，只有当前账号解锁后才能看到
- 统一好友与聊天展示名优先级：好友列表、好友详情、好友申请、聊天页标题与群成员列表统一按 `备注昵称 > 昵称 > 用户名 > 邮箱` 展示，减少不同页面名称不一致的问题
- 新增群管理设置入口：群管理现在可以在群信息页修改群名称、转让群管理，修改结果会通过端到端加密群控制消息同步给相关成员
- 密码库分类改为树形结构：密码库主页、分类管理页和密码编辑页统一支持父子分类展示与选择；选择父分类时会连同子分类一起筛选密码项，并增加循环层级保护，避免把分类挂到自己的子分类下面

### Fixed

- 修复个人资料更新时的头像误重置：后端现在只会在请求明确携带头像预设时才更新头像，单独修改昵称不会再把头像悄悄重置成默认值
- 收紧桌面端实时重连节流：前端现在会合并重复的“网络恢复 / 回到前台”实时恢复请求，并对强制重连增加冷却时间，避免 Windows 等桌面环境因网络状态抖动触发 websocket 重连风暴，导致界面卡顿、在线状态乱跳和消息发送异常
- 降低实时状态提示抖动：重复的“正在重连 / 已断开 / 已恢复”文案不再每次都触发整页刷新，减少聊天页和主页在实时链路波动时的无效 rebuild
- 补强桌面端连接设置持久化：后端地址和主题会额外写入用户级持久配置文件，桌面应用卸载后重新安装时，只要用户目录未清理，仍可自动恢复上次保存的连接地址
- 聊天列表红点改为真正的未读数：只统计“别人发给当前账号且尚未读取”的消息；进入会话页或在当前会话中收到新消息时会自动标记为已读，不再把待发送或待送达消息误显示成红点

### Verified

- `securex-be`: `go test ./...`
- `securex-app`: `flutter analyze`

## [v1.0.12] - 2026-05-26

### Fixed

- 优化单聊实时链路：服务端在写入加密待拉取队列后，会优先把同一份密文信封实时直推到在线目标设备；前端收到后立即解密、落会话并确认，减少在线单聊依赖轮询补拉导致的延迟、丢通知和“必须下拉刷新才出现”的情况
- 修复实时新消息被旧会话详情覆盖的问题：前端加载会话详情时新增归档版本保护，遇到旧版本详情不再回滚当前会话消息，而是按增量合并保留更新后的实时消息
- 修复聊天归档旧版本倒灌：后端保存整包归档和按会话归档时会忽略更旧的版本，避免并发同步或弱网重试把新消息覆盖成旧快照
- 优化在线状态校准：新增实时在线状态快照接口，前端在加载好友、打开会话和实时重连后会主动拉取一次 presence 快照，修正仅依赖 websocket 广播时可能出现的红绿点不准问题
- 修复“刚成为好友时双方都已在线却不显示在线”的时序问题：服务端在好友申请同意后会主动互推一次当前在线状态，前端收到好友关系变化后也会补拉一次在线状态快照
- 降低前台兜底轮询频率：在保留弱网补拉能力的同时，把待处理聊天消息轮询从高频补拉调整为“实时直推优先、低频补拉兜底”，减少无效请求和状态抖动
- 修复聊天消息误确认导致的潜在丢消息：当客户端虽然已解密密文，但好友/群上下文尚未准备好时，不再提前 ack 删除服务端待拉取消息；只有真正入会话成功或明确属于重复消息时才确认，避免“显示已发送但对方刷新也收不到”的硬丢失
- 补强服务端在线状态记录：实时连接在 connect、ping/pong 和正常收发期间都会刷新设备最近活动时间，presence 快照同时返回在线状态和最近活动时间，便于后续展示“刚刚在线”等信息

### Updated

- 新增本地实时聊天冒烟脚本 `securex-app/tool/chat_realtime_smoke.dart`：使用 `test / test2 / test3` 测试账号自动校验好友在线状态、在线消息直推、多设备同步、单聊/群聊离线补拉、送达确认与群解散清理，方便后续本地回归聊天链路

### Verified

- `securex-be`: `go test ./...`
- `securex-app`: `flutter analyze`
- `securex-app`: `dart run tool/chat_realtime_smoke.dart`

## [v1.0.13] - 2026-05-27

### Fixed

- 修复反向代理子路径部署下的实时地址生成：后端 `realtime/config` 现在会优先识别 `X-Forwarded-Prefix`，并支持 `server.publicBasePath` 兜底，避免部署在 `/securex-be` 这类前缀路径时 websocket 连接错地址，导致在线状态不更新、消息退化为待拉取
- 补强聊天弱网兜底：客户端在实时 websocket 暂时断开后不再停止待收消息补拉，而是继续按固定间隔异步拉取加密消息并刷新在线状态，降低公网代理抖动或移动网络切换时出现“对方在线却要等很久才收到消息”的概率

### Verified

- `securex-be`: `go test ./...`
- `securex-app`: `flutter analyze`

## [v1.0.11] - 2026-05-26

### Updated

- 单聊好友信息页新增“清除聊天记录”能力：当前账号可只删除自己与该好友的本地缓存和服务端加密归档中的单聊记录，不影响好友侧已有聊天记录
- 群聊信息页新增“清空聊天记录”能力：当前账号可只清除自己在该群中的本地缓存和服务端加密归档记录，不退出群聊，也不影响其他成员已有消息
- 优化登录与解锁过程反馈：登录页和解锁页改为实时监听控制器状态，解锁时会分阶段显示“验证密码 / 恢复保险库 / 同步聊天归档 / 即将进入主页”等进度提示，减少界面长时间无反馈的感受
- 新增“解散群聊”能力：仅群管理可解散群；解散后群管理自己的本地会话和服务端加密归档立即删除，其他成员仍会看到“已解散”的只读群会话，并可自行删除当前账号的群历史与占位记录

### Fixed

- 修复 `scripts/release-preflight.sh` 在 `pipefail` 场景下对已提交 changelog 的误判，避免发版前校验错误地提示“当前 HEAD 尚未包含对应版本 changelog”

### Verified

- `securex-be`: `go test ./...`
- `securex-app`: `flutter analyze`

## [v1.0.10] - 2026-05-26

### Fixed

- 修复 GitHub Actions Windows 发版任务误失败：`Flutter Windows x64` 的 `Build Windows`、打包和产物上传本身都已成功，失败点实际在 `subosito/flutter-action` 的 `Post Set up Flutter` 缓存回写；release workflow 现已关闭 Flutter action 缓存，避免发布流程被后置缓存步骤误判为失败

## [v1.0.9] - 2026-05-26

### Fixed

- 修复前端发版版本号不同步：Flutter release workflow 现在会把 `v*` tag 注入各平台构建的应用内部版本，避免安装包文件名已经升级但应用内仍显示旧版本号
- 调整版本更新页显示：用户侧“当前版本”优先展示语义版本号，不再把内部构建号作为主版本文案
- 收紧聊天实时重连条件：前端网络监听改为只在“离线恢复到在线”时主动重建实时通道，避免桌面端频繁 connectivity 事件触发重复重连，导致好友在线状态抖动、`chat-pending` 通知窗口丢失和消息送达体验异常
- 调整好友在线状态判定：聊天页红绿点只以服务端 `presence-online / presence-offline` 为准，不再混入通道内部 `ready` 状态，减少“明明在线却显示离线”或频繁跳变的问题
- 新增聊天会话页下拉刷新：单聊和群聊窗口现在支持直接下拉刷新，会同步聊天归档、待处理消息与当前会话历史详情
- 优化聊天会话页刷新体验：下拉刷新完成后会在后台继续补载当前会话详情，不阻塞当前页面交互与滚动

### Verified

- `securex-app`: `flutter analyze`


## [v1.0.8] - 2026-05-25

### Updated

- 补强聊天实时兜底：客户端在实时加密通道已连接后增加前台定时补拉待处理密文消息，断线、锁定或退出登录时自动停止，降低移动端偶发漏收消息后必须手动刷新才能看到新消息的问题
- 统一聊天消息状态文案：会话详情与会话列表中的状态提示改为 `待发送 / 已发送 / 已送达`，减少“等待确认”这类不够直观的描述
- 统一子页面切换动效：带返回导航的独立子页面改为从右向左滑入、返回时从左向右滑出，交互更接近移动端原生体验
- 新增群聊列表下拉刷新：好友页中的 `群聊` 页面两个页签现在都支持和“新的朋友”一致的下拉刷新，空列表状态下也可直接触发刷新

### Verified

- `securex-be`: `go test ./...`
- `securex-app`: `flutter analyze`

## [v1.0.7] - 2026-05-25

### Updated

- 优化聊天页加载性能：客户端进入聊天窗口时改为“会话摘要先显示、会话详情按需解密加载”，减少手机端打开长会话时的首屏等待
- 重构本地聊天缓存结构：本地聊天缓存拆成“摘要清单 + 单会话详情”两层密文文件，并继续兼容旧版整包密文快照自动迁移，降低内存占用同时确保升级不丢消息
- 收紧聊天归档同步一致性：消息接收、历史同步、送达回执、待发补发和群聊退出等路径统一先补齐会话详情，避免把摘要态消息误回写成完整归档或遗留脏缓存
- 补强发版防呆：新增 `scripts/release-preflight.sh` 本地预检脚本，并更新 release workflow 在手工触发时按目标 tag 检出源码、校验 tag 与当前提交一致，降低误把新版本 tag 推到旧提交上的风险

### Verified

- `securex-be`: `go test ./...`
- `securex-app`: `flutter analyze`
- `scripts/release-preflight.sh`: `bash -n`

## [v1.0.6] - 2026-05-25

### Updated

- 修复桌面端下拉刷新交互：应用全局滚动行为新增鼠标拖拽支持，桌面端可像移动端一样用鼠标按住内容区下拉触发聊天页、好友页等 `RefreshIndicator`
- 调整聊天密文存储策略：客户端聊天恢复改为“服务端加密归档优先、本地缓存兜底”，手动刷新与登录解锁都会先拉取当前账号的服务端密文归档，再合并本地未同步消息并回写加密归档
- 统一聊天相关文案与设计文档：前端页面、README、架构文档和安全规则不再把聊天描述为“仅保存在本机”，而是明确为“服务端只保存不可解密密文归档和密文信封，本地只保留缓存”
- 新增按会话增量聊天归档：后端增加会话归档清单与按会话密文详情接口，客户端新增归档版本号、按会话脏标记和增量同步逻辑；旧整包密文归档继续保留为兼容读取和迁移兜底，避免升级过程中丢历史消息
- 重构“新的朋友”页面：好友申请页改为“收到的申请 / 发出的申请”双页签结构，和群聊列表交互保持一致，每个页签都支持独立下拉刷新与空态展示

### Verified

- `securex-be`: `go test ./...`
- `securex-app`: `flutter analyze`

## [v1.0.5] - 2026-05-23

### Updated

- 优化手机端进入聊天窗口的首屏体验：会话列表进入聊天页时不再同步等待实时通道建立，改为先打开页面、再后台串行预热聊天连接与待同步消息，减少移动端点击会话后的明显卡顿
- 优化聊天详情页消息渲染：消息列表改为 `ListView.builder` 按需构建，避免历史消息较多时在手机端首帧一次性构建全部气泡导致掉帧
- 新增好友页与聊天页下拉刷新：用户可主动异步拉取好友列表、群聊元数据、聊天归档、待同步消息与在线历史同步请求，不再依赖顶部单独的同步按钮
- 精简好友页头部操作：移除和下拉刷新重复的“同步”按钮，并统一好友详情、群聊列表、单好友成组聊天入口的打开逻辑，避免手机端再次出现进入聊天前等待实时连接的卡顿
- 收紧前端重建范围：应用壳层改为只在主题、初始化、登录态和解锁态变化时重建，聊天页和好友页改为只监听各自域状态，减少消息收发、好友同步、上传进度等无关状态触发的整页重绘

### Verified

- `securex-be`: `go test ./...`
- `securex-app`: `flutter analyze`

## [v1.0.4] - 2026-05-23
### Updated

- 新增服务端群聊元数据存储：后端增加群房间、群成员关系和“当前用户自己的加密群快照”接口，客户端解锁后可从服务端恢复我创建/我加入的群聊列表，同时服务端仍不保存可解密聊天正文
- 新增用户级加密聊天归档：客户端会把自己的聊天快照再次加密后同步到服务端，供同账号其他设备解锁后恢复；服务端只能看到密文归档，不能解密正文
- 收敛图文聊天主链路：实时配置与客户端实现改为 `WebSocket + 应用层端到端加密` 为主，不再把 WebRTC 作为普通图文聊天默认主通道
- 新增设备级聊天身份与密文队列：客户端为每个用户设备持久化独立身份种子与公钥，后端保存设备目录与短期待拉取的不可解密密文信封，支持移动端重连后自动拉取待同步消息
- 重构单聊、群聊、历史同步与送达回执链路：全部切到 `ChatProtocol` 抽象层和按设备逐个加密投递的模式，方便后续替换协议实现而不破坏现有架构
- 收紧聊天身份种子调试兜底：当开发环境系统安全存储不可用时，聊天设备身份种子改为使用保险库密钥做本地再加密保存，避免直接落到明文调试存储
- 修复多前端联调脚本并发构建冲突：`start-dev-app.sh` 在 macOS 下对 `flutter build macos --debug` 增加构建锁，避免双实例或多实例同时启动时触发 `XCBuildData/build.db` 锁冲突
- 加固实时信令授权：后端实时信令不再只允许好友互通；存在共同群聊的成员也可建立实时端到端会话或走在线密文中继，但这不会授予任何保险库或文件访问权限
- 优化移动端恢复策略：客户端在锁屏返回前台、网络恢复、Wi‑Fi/蜂窝切换后会强制重建实时连接，并自动补发本机未送达消息、向在线好友或共同群成员发起历史同步
- 调整群聊多端恢复边界：群消息仍优先保存在客户端本地；服务端只保存每个用户各自不可由服务端解密的加密群快照，以及短期设备密文信封，不作为可解密聊天离线消息库
- 补充升级兼容约束：聊天协议、客户端本地快照、服务端密文队列和群元数据都要求版本化和增量迁移，避免后续升级导致用户聊天数据丢失

### Verified

- `securex-be`: `go test ./...`
- `securex-app`: `flutter analyze`

## [v1.0.3] - 2026-05-23

### Updated

- 修复 GitHub Actions 的 macOS/iOS 构建失败：将 `connectivity_plus` 精确锁定到兼容 Xcode 16.4 runner 的 `6.1.5`，避免 `7.1.1` 在 Apple 平台编译时使用 `NWPath.isUltraConstrained` 导致 `Flutter macOS x64`、`Flutter macOS arm64` 与 `Flutter iOS unsigned` 任务失败

## [v1.0.2] - 2026-05-22

### Updated

- 修复后端实时信令 WebSocket 稳定性：为信令连接补充独立心跳、读超时续期以及控制帧/业务帧串行写入，降低 1 分钟左右异常断线和好友误判离线的问题
- 优化实时信令日志：后端在 WebSocket 读取失败、写入失败和心跳发送失败时输出更明确的中文错误，便于继续定位断线根因
- 优化移动网络实时聊天策略：客户端新增应用回前台、网络恢复与 Wi‑Fi/蜂窝切换后的自动重连与会话恢复，降低手机端切网后长期离线的问题
- 新增加密 WebSocket 在线中继兜底：当 WebRTC DataChannel 因移动网络或 NAT 不稳定无法建立时，聊天、群控与历史同步会自动回退到“客户端加密、服务端只转发密文”的在线中继模式，服务端仍不保存消息正文
- 修正好友在线状态判定：好友红绿点不再错误依赖 WebRTC 通道状态，而是以实时 presence 和加密会话就绪状态为准
- 修复密码生成器子页面按钮无响应：生成器页改为独立状态组件，预设、字符集、长度滑块、重新生成与复制操作都会立即刷新界面

## [v1.0.1] - 2026-05-22

### Updated

- 统一发布命名：后端 release 包内可执行文件改为 `secure-x`，前端 Android、iOS、macOS、Windows、Linux 应用显示/窗口名统一为 `secure-x`
- 补齐客户端平台权限：Android release 主 manifest 增加联网与网络状态权限，并仅允许本机/模拟器调试地址使用明文 HTTP；iOS 增加本地网络访问说明与本地 HTTP 访问例外
- 新增设置页 `关于`：展示应用图标、当前版本、版本介绍与版本更新入口，参考移动端关于页布局
- 新增版本更新能力：客户端可从 `ligson/secure-x` GitHub Release 检查最新版本，按当前平台选择安装包下载；桌面端和 Android 下载后打开安装文件，iOS 因系统限制跳转 Release 页面
- 新增 `package_info_plus`、`url_launcher`、`open_filex` 依赖，用公共库处理版本读取、外部链接和安装包打开
- 新增后端日志配置：`logging.dir`、`logging.appFile`、`logging.accessFile`，应用日志与 Gin 访问日志默认写入日志目录并同时输出到控制台
- 新增后端生产部署辅助文件：`start.sh`、`stop.sh`、`status.sh` 与 `secure-x.service`，并在 release 打包时一并放入后端压缩包
- 优化开发脚本：本地后端调试二进制改为 `.dev/bin/secure-x`，开发配置自动写入日志目录配置
- 补充中文日志与注释：后端启动、数据库、文件目录、实时信令连接/断开/投递失败输出中文日志；前端新增统一本地日志 helper，现有调试日志改为中文
- 更新发版协作规则：后续用户说“发版”时默认基于当前最新 `v*` tag 自动递增 patch 版本，例如 `v1.0.0` 后下一版为 `v1.0.1`
- 调整 changelog 规范：发布章节必须同时包含版本与日期，格式为 `## [vX.Y.Z] - YYYY-MM-DD`，方便 GitHub Release 精准读取对应版本内容
- 调整 GitHub Actions Release 说明提取逻辑：优先按当前 tag 匹配 `CHANGELOG.md` 的对应版本章节，避免误把未发布内容写入 Release

## [v1.0.0] - 2026-05-21

### Updated

- 新增 GitHub Actions `Release` workflow：推送 `v*` 标签后自动构建后端 Linux/macOS 包与 Flutter Windows/macOS/Android/iOS 产物，并上传到 GitHub Release
- 后端发布包统一输出 `tar.gz`，覆盖 `linux-amd64`、`linux-arm64`、`macos-amd64`、`macos-arm64`，包内包含二进制、示例配置与后端 README
- 前端发布产物覆盖 Windows x64、macOS x64、macOS arm64、Android APK/AAB 与未签名 iOS app bundle，Release 内容自动从 `CHANGELOG.md` 最新章节读取
- 新增 `doc/06-release.md`，记录多端打包产物、tag 触发方式、iOS 签名边界与发布安全注意事项
- 新增 `securex-app` 聊天页右上角加号菜单，支持 `发起群聊` 与跳转 `添加好友`
- 新增 `securex-app` 发起群聊页面：选择 1 个好友进入单聊，选择多个好友时要求填写群聊名称并创建群会话
- 新增 `securex-app` 本机加密群聊 MVP：群会话元数据、成员列表与消息快照只在客户端本机加密保存，服务端不保存群消息
- 扩展 WebRTC 聊天投递：群消息通过现有好友端到端通道逐个成员发送，接收方可根据群元数据建立本地群会话
- 新增群消息按成员记录已发送/已送达状态，成员上线后可继续补发未送达的本机加密群消息
- 新增群聊信息页：群主可移除成员，成员可邀请自己的好友进群，后续消息按当前本机成员列表发送
- 修复好友申请同意后的跨客户端同步：后端通过实时通道推送好友关系变化，客户端自动刷新好友/申请快照，避免申请方仍显示等待
- 修复刚成为好友后收不到聊天消息的问题：客户端收到实时消息时若本地好友快照滞后，会先刷新好友列表再处理消息，避免误丢弃合法好友消息
- 新增好友页 `群聊` 入口：位于 `新的朋友` 下方，进入后按 `我创建的群聊` / `我加入的群聊` 两个页签展示本机加密群会话，并可点击进入群聊
- 新增群设置 `退出群聊`：退出前二次确认，退出后删除本机该群会话和全部聊天记录；群管理退出时按成员顺序自动转交给下一位成员，并通过端到端通道通知在线成员更新
- 新增端到端聊天历史同步 MVP：登录解锁后自动连接好友，实时通道就绪后向在线好友请求历史消息，并从好友或共同群成员本机同步单聊/群聊历史，服务端仍不保存聊天消息
- 重构后端配置：新增 `securex-be/config.example.yaml` 与 `--config` 启动参数，后端不再读取 `SECUREX_*` 环境变量，开发脚本改为生成 `.dev/securex-be.yaml` 后启动
- 更新协作规则：后续由我重启本地测试前端实例时，若实例对应已记录测试用户，应尽量帮用户登录并解锁到主界面，方便直接联调测试
- 更新 `README.md`、`AGENTS.md` 与 `doc/`，记录群聊不落服务器、当前 MVP 边界，以及后续群历史同步和密钥轮换要求
- 拆分开发启动脚本：新增 `start-dev-be.sh`、`start-dev-app.sh`、`start-dev-all.sh` 与对应 `stop-dev-*.sh`，旧 `start-dev.sh` / `stop-dev.sh` 保留为全量启动/停止兼容入口
- 调整 `start-dev-app.sh` 支持多前端实例：重复运行会启动新的客户端实例，也可通过 `APP_INSTANCE=user-a` / `APP_INSTANCE=user-b` 指定稳定实例名，方便调试双用户聊天
- 加固 `securex-app` 本地调试数据隔离：按 `SECUREX_DEV_INSTANCE` 隔离后端地址、token、主题、本机聊天密文队列等本地数据，避免多个前端实例串用户数据
- 更新协作规则：后续调试启动、停止、重启前后端或多前端实例时，必须和用户完全使用同一套项目脚本入口，不绕过脚本直接运行底层命令
- 更新联调协作规则：调试前先检查并优先复用用户已启动的可用实例，只有缺失、异常或需要额外隔离前端时才通过脚本启动新实例
- 记录本地联调测试账号：`test` 与 `test2`，用于后续好友和聊天双用户测试，凭据仅保存在协作记忆中不写入对外 README
- 修复 `securex-app` 聊天发送后当前窗口与会话列表不立即刷新的问题：发送时先更新内存会话并通知 UI，再异步加密落盘
- 优化 `securex-app` 聊天消息状态展示：消息下方增加未送达/待发送/已发送状态图标，未送达或待发送消息可点击 `重发`
- 新增 WebRTC 加密聊天实现：后端提供已认证的 WebSocket 信令转发且不存消息，客户端通过 `flutter_webrtc` 建立 DataChannel，并用 X25519 派生会话密钥后对聊天正文做 AES-GCM 应用层加密
- 新增聊天送达确认：实时消息经 DataChannel 发送后先标记 `已发送`，收到对端 ack 后更新为 `已送达`
- 新增聊天待发自动刷新：WebRTC 通道打开后会尝试发送本机会话中未送达/待发送消息，仍保留手动 `重发`
- 优化聊天实时连接：移除聊天页手动 `连接` 按钮，改为登录解锁后自动连接、WebSocket 心跳与断线重连
- 修复 WebRTC 信令处理：串行处理信令消息，补充候选队列与空连接保护，避免 offer/candidate 到达时空指针导致无法收消息
- 修复 macOS 本机聊天密文队列写入失败：沙盒环境下回退到应用支持目录，并继续按调试实例隔离本地数据
- 优化聊天窗口视觉：按主题变量重绘聊天背景、气泡、头像、状态条与输入栏，提高图标文字对比度并适配浅色/深色主题
- 优化聊天页提示位置：聊天相关状态不再占用全局顶部提示区，改为标题下方的可关闭页内提示
- 统一主工作区提示交互：密码库、文件、好友、设置页也改为页面内容里的可关闭提示，不再使用顶端全局提示框
- 新增聊天好友在线状态提示：聊天窗口好友名称后显示绿点/红点，基于 WebRTC 实时通道状态展示在线或离线
- 修复双端同时打开聊天时的 WebRTC offer 撞车问题：服务端广播好友 presence，客户端用固定发起者规则和 connect-request 建立通道，避免双方同时 setRemoteDescription 失败
- 调整主页面布局：密码库、文件、聊天、好友、设置页统一为顶部标题区固定、底部导航固定，仅中间内容区滚动
- 修复 `start-dev-app.sh` macOS 多实例启动方式：改为通过 `open -n --env` 启动 `.app` bundle 并记录真实 GUI 进程，避免直接执行二进制后窗口进程退出
- 新增 `securex-be` 好友通讯录接口：支持发送好友申请、查看收到/发出的申请、同意、拒绝、删除好友，好友关系必须经对方同意后才建立
- 新增 `securex-app` 好友主导航页：按类似微信通讯录的结构展示新的朋友、添加好友、搜索好友、分组好友列表、好友详情与删除好友
- 精简 `securex-app` 好友页入口：移除顶部重复的添加按钮，保留通讯录操作区里的 `添加好友` 入口
- 新增 `securex-app` 聊天主导航：位于 `文件` 与 `好友` 之间，支持会话列表、聊天详情、好友详情发起聊天与类似移动聊天工具的输入区
- 新增 `securex-app` 本机加密消息队列：实时通道未建立或好友未在线时，待发消息只使用保险库密钥加密保存在本机，不上传服务器保存
- 新增 `securex-be` 实时服务配置接口：客户端通过 HTTP 获取 WebRTC/信令配置，避免写死 socket 端口或服务地址
- 更新聊天安全设计文档：明确聊天消息端到端加密、服务端不存离线消息，后续实时传输优先采用 WebRTC 数据通道
- 更新安全文档与协作规则：明确好友关系只代表通讯录关系，不代表密码库或文件授权，不能让对方读取、下载或解密用户个人密文资源
- 调整 `securex-app` 生成器入口：从底部主导航移到密码库页面顶部，底部导航聚焦为密码库、文件、好友和设置
- 加固 `securex-be` 写接口所有权校验：创建/更新密码分类、文件目录、密码项、文件元数据、普通上传与分片上传时，会拒绝引用其他用户的分类或目录
- 新增 `securex-be` 访问控制回归测试：验证用户不能引用其他用户目录，也不能下载其他用户的密文文件
- 更新安全规则文档：明确 Secure X 按超级安全系统设计，任何用户数据只能由所属用户自己的客户端解密，其他用户和服务端都不能解密看到
- 新增 `securex-app` 品牌应用图标：采用深青蓝安全底色、保险库盾牌与 `X` 标识，替换 Flutter 默认图标
- 接入 `flutter_launcher_icons` 生成 Android、iOS、macOS 与 Windows 启动图标，并保留 `assets/brand/securex_app_icon_square.png` 作为统一源图
- 重构前后端代码组织：后端 `httpapi` 从单体路由文件拆分为认证、分类、条目、文件、分片上传、同步与公共 helper 等领域文件
- 重构 `securex-app` 页面组织：`main.dart` 拆分为认证、解锁、密码库、生成器、文件、设置、编辑页与通用组件等小文件
- 重构 `securex-app` 控制器组织：`AppController` 按认证、保险库、上传、记录操作、会话与内部工具方法拆分，主控制器仅保留状态字段与基础 getter
- 更新 `AGENTS.md` 与架构文档，记录前后端新增功能应按模块/页面/领域拆分，避免继续形成千行大文件
- 拆分 `securex-be` 密码库分类与文件目录模型：新增独立 `file_folders` 目录资源与 API，文件模块不再复用密码分类
- 重构 `securex-app` 文件页为网盘式当前目录视图：支持进入目录、返回上级、新建目录、编辑目录、删除空目录，并默认上传到当前目录
- 修复 `securex-app` 文件上传体验：选择文件取消或文件选择器异常时会显示明确状态提示，不再表现为“点了没反应”
- 补充 `securex-app` macOS 沙盒文件读取权限，允许用户选择本地文件后由客户端加密上传
- 修复 `securex-app` macOS 主窗口启动可见性：补充窗口启动显示配置，并在应用启动完成后显式拉起主窗口
- 优化 `scripts/start-dev.sh` macOS 客户端启动方式：通过 app bundle 启动并回填真实 GUI 进程 pid，避免脚本记录短生命周期 bootstrap pid
- 修复 `securex-app` 文件页底部溢出：列表区域不再强制占用剩余高度，改为固定高度的可滚动内容块，适配较小桌面窗口和移动端高度
- 优化 `securex-app` 密码库分类与生成器字符集按钮的选中态配色：改为浅青底、深青文字与深青勾选图标，提升可读性
- 重构 `securex-app` 颜色体系：新增 `SecureXThemeSpec` 与 `SecureXPalette` 主题变量，统一背景、卡片、边框、按钮、Chip、提示条等主要颜色来源
- 新增 `securex-app` 设置页主题选择：提供 `晨雾白`、`亚麻暖白`、`松林夜`、`星港夜` 四套主题，并持久化到本地偏好设置
- 修正 `securex-app` 主题选择入口位置：主题外观只放在 `设置` 页，不再出现在 `生成器` 页
- 调整 `securex-app` 登录后主导航：移除顶部标题与顶部页签，改为底部 `密码库 / 生成器 / 文件 / 设置` 导航，减少顶部空间占用
- 重构 `securex-app` 设置页信息架构：设置首页改为分类列表，`个人信息`、`主题外观`、`连接设置`、`会话操作` 分别进入独立子页面配置
- 修复 `securex-app` 会话操作跳转：在设置子页面点击 `锁定` 或 `退出登录` 后会主动返回主导航根页面，并立即显示解锁页或登录页
- 优化登录/注册失败提示：后端认证、注册冲突、Token 失效等错误改为中文响应，前端网络异常与解锁密码解锁异常也不再直接展示库的英文原文
- 调整 `securex-app` 登录与解锁流程：用户侧统一称为 `解锁密码`，登录页只输入账号登录密码，登录成功后再进入解锁页输入解锁密码
- 优化 `securex-app` 解锁页操作区：`解锁` 与 `退出登录` 改为同一行按钮布局，界面更紧凑
- 新增 `securex-be` 文件分片上传协议：支持创建上传会话、上传密文分片、查询已上传分片与完成后服务端合并密文文件
- 优化 `securex-app` 大文件上传体验：文件上传改为应用内后台任务，上传期间展示进度，不再用全局 busy 阻塞密码库、生成器、文件浏览和设置操作
- 优化 `securex-app` 上传性能：文件读取改为随机访问按块读取，分片加密迁移到 isolate，减少大文件上传时的界面卡顿
- 优化 `securex-app` 上传进度交互：上传完成或失败后可点击关闭按钮收起对应进度记录
- 调整 `securex-app` 文件加密上传：文件按分片在客户端加密并上传，下载时兼容分片文件与旧版单体密文文件
- 更新项目文档与协作记忆，明确大文件上传应使用后台任务、进度展示与分片上传，重启后自动断点恢复作为下一步增强
- 新增 `securex-be` 解锁密码配置更新接口：登录用户可提交客户端重新封装后的保险库密钥配置，服务端仍只保存密文
- 重构 `securex-app` 设置页密码入口：新增 `安全设置`，将 `登录密码` 与 `解锁密码` 修改统一收纳到安全设置下
- 新增 `securex-app` 解锁密码修改页面：本地校验当前解锁密码后，用新解锁密码重新封装保险库密钥并同步密文配置
- 新增 `securex-be` 登录密码修改接口：登录用户可通过当前登录密码校验后更新服务端认证密码，响应仍保持统一 JSON 结构
- 新增 `securex-app` 设置页 `登录密码` 修改页面：独立子页面支持当前登录密码、新登录密码、确认密码输入与本地校验，并明确不修改解锁密码和保险库加密密钥
- 更新 `README.md`、`AGENTS.md`、`doc/` 与子工程 README，记录登录密码修改能力与解锁密码不受影响的安全边界
- 新增 `securex-app` 密码库分类管理入口：支持集中新增、修改、删除分类，删除前会判断分类下是否还有密码或子分类并给出提示
- 精简 `securex-app` 创建/编辑密码页面：移除内嵌创建分类入口，分类新增、修改、删除统一从密码库 `分类管理` 进入
- 优化 `securex-be` 密码分类接口提示文案：分类删除冲突改为中文业务提示，继续通过统一 JSON 结构返回
- 更新 `README.md`、`AGENTS.md`、`doc/` 与客户端 README，记录密码库分类管理与非空分类不可删除规则
- 更新 `AGENTS.md` 前端规则：新增页面默认从主题变量取色，避免继续散落硬编码颜色，并要求主题覆盖日间与夜间可读性
- 更新项目文档与协作记忆，明确密码分类与文件目录分离，文件目录/文件元数据仍必须客户端加密
- 调整 `securex-app` 密码详情页的密码展示：默认不再直接明文显示，改为掩码展示，并补充复制与查看/隐藏图标
- 更新项目协作规则：前端 `创建 / 编辑 / 详情` 子流程默认优先使用独立页面承载，并通过顶部返回按钮回到上一层，减少悬浮弹窗式交互
- 调整 `securex-app` 现有子流程交互：文件夹编辑、文件编辑已从 `AlertDialog` 改为独立页面，和密码详情、密码编辑保持一致的返回方式
- 修复 `securex-app` 创建密码时的 Flutter 断言崩溃：将密码创建与详情查看从对话框改为独立页面，避免弹出流程触发 `_dependents.isEmpty` 断言
- 重构 `securex-app` 密码库页交互：默认改为展示搜索框、已有分类和密码标题列表，移除首页内嵌创建表单
- 为 `securex-app` 密码库补充独立创建流程：点击创建按钮后打开密码创建页面，统一包含分类选择和密码表单
- 为 `securex-app` 密码标题补充详情视图：点击标题可查看分类、用户名、密码、地址、备注，并在详情中执行删除或编辑
- 为 `securex-app` 登录与注册页的登录密码、解锁密码输入框补充查看密码图标，支持在输入时切换明文 / 密文显示
- 修复 `securex-app` 认证页输入框文案显示：登录与注册页的用户名、邮箱、密码等字段改用占位提示，避免扁平样式下浮动标签压到边框导致显示错位
- 精简 `securex-app` 视觉风格：整体改为更扁平的浅色界面，收紧圆角、边框、按钮与输入框层级，减少装饰性块和过重的卡片感
- 继续收敛 `securex-app` 登录与解锁页：保留产品名、后端地址和核心操作，移除多余说明，使桌面端与移动端入口更轻量
- 重排 `securex-app` 登录后四大模块的视觉结构：为 `密码库 / 生成器 / 文件 / 设置` 增加统一的扁平化图标标题头，列表和分类改为更轻的平面块样式
- 压缩 `securex-app` 文案密度：减少解释型安全说明卡片，把必要说明收成短标签或单行提示，让界面更简洁
- 重做 `securex-app` 登录入口：改为单卡片极简布局，仅保留 `secure-x`、一句安全说明、后端地址配置以及登录 / 注册表单，不再在未登录页堆叠宣传内容
- 重构 `securex-app` 登录后主页信息架构：统一整理为 `密码库 / 生成器 / 文件 / 设置` 四个主模块，桌面端与移动端共用同一套响应式代码
- 调整 `securex-app` 密码库与文件页交互：补充搜索、文件夹筛选与分类视图，让密码项与加密文件都围绕用户自建文件夹组织
- 增强 `securex-app` 密码生成器：支持长度、大小写、数字、符号等组合参数，生成逻辑仍仅在客户端本地执行
- 将安全说明从登录页宣传区迁移到登录后工作区：在密码库、文件、设置中明确强调“客户端加解密、服务端仅存密文”
- 修复 `securex-app` 认证区桌面布局溢出问题：去掉固定桌面高度约束，改为可滚动的自适应双栏布局，避免 `BOTTOM OVERFLOWED` 导致注册区和状态提示被裁切
- 增强 `securex-app` 登录与注册页本地校验：提交前统一校验后端地址、邮箱格式、登录密码长度、解锁密码长度与必填项，减少无效请求直达后端
- 优化 `securex-app` 认证提示链路：认证页增加本地状态提示复用，提交前的校验失败会直接在当前页展示，不再表现为“点了没反应”
- 调整 `securex-app` 认证参数清洗：注册用户名、邮箱和登录标识在发请求前统一 `trim`
- 优化 `securex-be` 参数校验返回：注册、登录及部分写接口的 `400` 绑定错误改为更友好的中文提示，保持统一响应结构下的可读性
- 修复 `securex-app` 登录后工作区底部溢出：文件夹、登录项、文件三页改为可滚动布局，避免小窗口或低高度桌面环境下再次出现 `BOTTOM OVERFLOWED`
- 优化 `securex-app` macOS 调试体验：系统安全存储写入失败时不再把会话流程打断，而是保留当前已登录会话，并给出“重启后需要重新登录”的友好提示
- 记录 `flutter_secure_storage` 的 macOS 调试限制：本地无开发签名时不强行启用 Keychain Sharing，避免联调脚本构建失败
- 优化 `securex-app` 的 `macOS Debug` 登录持久化：当系统安全存储不可用时，自动回退到本地调试存储，避免联调期间应用重启后反复重新登录

### Verified

- `securex-be`: `go test ./...`
- `securex-app`: `flutter analyze`
- `securex-app`: `flutter test`
- `securex-be`: 冒烟验证修改登录密码后旧密码登录失败、新密码登录成功
- `scripts/start-dev.sh`: 成功拉起后端与 macOS 客户端，并确认 `securex_app` 存在 onscreen 主窗口
- `securex-be`: `curl /healthz` 返回 `200`
- `securex-be`: 无效邮箱注册返回 `{"success":false,"message":"请输入有效的邮箱地址","httpCode":400,"data":{}}`
- `securex-be`: 有效注册请求仍可返回 `201`
- `scripts/start-dev.sh`: 在回退无签名调试方案后可再次成功构建并启动 macOS 客户端

## [v0.1.0] - 2026-05-20

### Added

- 初始化项目级 `README.md`，说明项目定位、技术方向、核心原则和文档入口
- 初始化项目级 `AGENTS.md`，记录长期协作记忆、产品约束和文档维护规则
- 初始化 `doc/README.md`，建立文档索引
- 新增 `doc/01-product-overview.md`，沉淀产品范围与 MVP 功能设计
- 新增 `doc/02-architecture.md`，沉淀客户端、服务端和存储层架构方案
- 新增 `doc/03-crypto-storage.md`，沉淀端到端加密与文件存储策略
- 新增 `doc/04-roadmap.md`，沉淀建议迭代路线与下一步计划
- 新增 `doc/05-open-questions.md`，集中整理需要用户拍板的关键设计决策

### Notes

- 本次仅完成设计文档初始化，尚未开始任何业务代码实现

### Updated

- 在 `AGENTS.md` 中补充开发约束：实现阶段优先复用成熟公共库，尽量少写自研通用能力，避免重复造轮子
- 新增 `.gitignore`，预先忽略 Go / Flutter 构建产物、编辑器文件、本地数据库与潜在敏感文件
- 更新文档中的工程目录命名约定：后端目录为 `securex-be`，前端目录为 `securex-app`
- 更新根目录 `.gitignore`，补充忽略 `securex-be/data/`
- 更新 `AGENTS.md`，将项目阶段同步为第一版代码实现阶段
- 更新 `README.md`，补充当前已实现能力与启动方式
- 更新 `doc/03-crypto-storage.md`，说明第一版实际采用 `PBKDF2-SHA256` 派生主密钥，并保留后续升级到 `Argon2id` 的空间
- 更新 `doc/04-roadmap.md`，将“下一步”从脚手架阶段调整为基于现有 V1 基线的迭代任务

### Added

- 新增 `securex-be/` Go 后端工程，接入 `Gin`、`GORM(SQLite)`、`JWT`、`bcrypt`
- 新增后端健康检查、注册、登录、用户信息、同步导出、文件夹、登录项、密文文件上传下载接口
- 新增 `securex-app/` Flutter 多端工程
- 新增客户端后端地址配置、注册、登录、解锁密码解锁、保险库同步、文件夹创建、登录项创建、随机密码生成、加密文件上传与解密下载能力
- 新增客户端加密实现，包含 `PBKDF2-SHA256` 主密钥派生、`AES-256-GCM` 文本加密与文件加密
- 新增 `securex-be/README.md` 与 `securex-app/README.md`，补充子工程说明与运行方式
- 新增第二轮交互能力：文件夹搜索/编辑/删除、登录项搜索/编辑/删除、文件搜索/重命名/删除
- 新增后端文件元数据更新接口，以及文件夹“非空不可删”保护
- 新增统一响应封装规则：后端 JSON 接口统一返回 `success`、`message`、`httpCode`、`data`
- 新增 `scripts/start-dev.sh`、`scripts/stop-dev.sh` 和 `scripts/dev-common.sh`，方便本地联调前后端

### Verified

- `securex-be`: `go build ./...`
- `securex-be`: `go test ./...`
- `securex-app`: `flutter analyze`
- `securex-app`: `flutter test`
- `securex-be`: 手动冒烟验证 `healthz`、`register`、`login`

### Updated

- 删除 `securex-app/web/`，与“不考虑网页端”的项目边界保持一致
- 重构 `securex-be` 所有 JSON 接口响应为统一结构，并同步适配 `securex-app` 的解析逻辑
- 更新 `README.md`、`AGENTS.md`、`doc/02-architecture.md`、`securex-be/README.md`，写入统一接口返回规范
- 更新 `.gitignore`，忽略本地调试产生的 `.dev/` 目录
- 重做 `securex-app` 桌面登录页视觉设计，修复品牌区溢出问题，并优化桌面端布局、输入区层级与品牌展示
- 调整 `securex-app` 登录页为真正的响应式布局，修复桌面端空白页问题，并让桌面与手机端共用同一套自适应代码
- 修复 `securex-app` 认证页运行时白屏问题：移除 `google_fonts` 在线字体依赖，并重写 `_HeroPanel` 指标区布局，避免桌面端触发无限高度约束异常
- 强化本地联调脚本：统一要求通过 `start-dev.sh` / `stop-dev.sh` 管理前后端，并在脚本中补充残留进程清理、macOS 客户端真实进程识别与单实例保障逻辑
- 修复联调脚本 PID 复用误判问题：PID 文件除“进程存活”外还必须匹配本项目命令，避免 `start-dev.sh` 误报前端已运行，或 `stop-dev.sh` 误杀无关进程
- 优化 macOS 联调启动体验：`start-dev.sh` 检测到前端已运行时会尝试激活前台窗口，新启动时会把 PID 文件回填为真实 GUI 进程
- 优化联调可用性：`start-dev.sh` 现在会等待后端 `healthz` 就绪后再返回，客户端连接失败时也会显示更明确的后端不可达提示
- 修复后端后台常驻问题：`securex-be` 改为通过独立 session 启动，避免 `bash start-dev.sh` 结束后后端进程被当前 shell 连带回收
- 修复 macOS 前端重复窗口问题：新启动客户端时不再额外执行一次 `open -a`，只在已运行场景下用于激活现有窗口
- 修复 macOS 桌面端网络权限：为调试与发布 entitlements 补充 `com.apple.security.network.client`，解决客户端无法连接本地后端的问题
