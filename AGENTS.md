# AGENTS.md

本文件用于记录本仓库的长期协作约束、产品记忆与文档维护规则，帮助后续继续工作时保持一致。

## 当前阶段

- 当前阶段为 `第一版代码实现阶段`
- 当前已从纯文档阶段进入“可运行基线实现”
- 后续优先在现有前后端骨架上迭代，不回退到重新起盘

## 产品定位记忆

- 项目名称：`Secure X`
- 项目目标：安全敏感信息存储维护系统
- 对标参考：`Bitwarden`
- 差异点：除密码/登录信息外，还要支持 `加密文件存储`
- 部署方式：优先支持私有部署

## 技术方向记忆

- 后端建议使用：`Golang`
- 后端目录名称：`securex-be`
- 客户端建议使用：`Flutter`
- 前端目录名称：`securex-app`
- 客户端目标平台：`macOS`、`Windows`、`Linux`、`Android`、`iOS`
- 不考虑网页端
- 前端需要支持填写与切换后端地址
- 服务端数据库建议：`SQLite`
- 文件内容存到服务端文件存储中，但保存的是 `密文`
- 第一版已实现：`Gin + GORM(SQLite) + JWT + bcrypt`
- 第一版客户端已实现：`Flutter + dio + cryptography + flutter_secure_storage`

## 安全原则记忆

- 本系统定位为 `超级安全` 的敏感信息存储系统，任何产品、架构、接口、UI、脚本与运维设计都必须优先考虑安全性
- 系统涉及敏感数据的能力默认都要遵循 `E2EE（端到端加密，End-to-End Encryption）` 思路设计：密钥生成、持有、协商、封装、解封装与明文恢复都优先限制在客户端，服务端默认只能接触不可解密密文与最小必要元数据
- 所有敏感数据必须 `客户端加密，客户端解密`
- 一个用户的数据永远只能由该用户自己的客户端在正确解锁后解密看到
- 其他用户、服务端、数据库管理员、文件存储管理员默认都绝对不能解密看到用户业务明文
- 服务端只负责：
  - 账号与认证
  - 密文元数据管理
  - 密文文件存储
  - 同步与版本控制
- 服务端默认不可接触任何业务明文
- 服务端接口必须同时做认证与所有权校验，不能让用户读取、下载、引用、修改或删除其他用户的密文资源
- 文件、密码项、备注、登录信息都应以密文形式传输与存储
- 所有客户端和服务端的数据结构、密文载荷、同步协议、聊天协议、快照文件和数据库表在设计时都必须考虑 `升级兼容`
- 默认不允许因为应用升级、协议切换、数据库迁移、配置调整、平台切换或脚本重启导致用户已有数据丢失；无论是客户端本地数据，还是服务端数据库、文件密文、密文队列、聊天归档和群元数据，都必须优先采用增量迁移和兼容读取
- 好友关系只代表通讯录关系，不代表保险库授权；添加好友不得让对方获取、读取、下载、解密或间接推断用户的密码库与文件明文
- 聊天消息也属于敏感数据，默认必须端到端加密；服务端不得存储聊天消息正文或可解密内容

## 计划中的核心能力

- 注册
- 登录
- 解锁密码体系
- 密码库分类
- 文件网盘目录
- 密码/登录信息管理
- 加密文件上传下载
- 好友通讯录与好友申请
- 加密聊天会话
- 随机密码生成器
- 多端同步

## 当前实现记忆

- `securex-be` 已实现：
  - 注册、登录、当前用户
  - 用户昵称与预置头像资料更新，头像预设由服务端白名单校验
  - 登录密码修改
  - 解锁密码密文配置更新
  - 密码库分类新增、更新、删除，删除时会阻止删除仍包含密码项或子分类的分类
  - 登录项新增、更新、删除
  - 文件目录新增、更新、删除
  - 好友申请发送、同意、拒绝、好友列表与删除好友
  - 群聊房间、群成员关系、当前用户群快照存储、群退出与群管理解散群
  - 用户级加密聊天归档存储，用于同账号多端恢复
  - 聊天附件密文对象上传与授权下载，服务端只保存不可解密密文
  - 实时服务配置查询，客户端不能硬编码 socket / 信令端口
  - 加密文件普通上传、分片上传、元数据更新、下载、删除
  - 写接口会校验目录/分类归属，禁止用户引用其他用户的密码分类、文件目录或上传任务
  - 实时信令同时校验好友关系或共同群成员关系，支持群成员建立实时端到端通道
  - 同步导出接口
- `securex-app` 已实现：
  - 后端地址配置
  - 注册、登录、登录密码修改、解锁密码解锁与修改
  - 个人预置头像修改，好友头像展示，群头像展示与群管理修改
  - 密码库分类创建、选择、修改、删除
  - 登录项搜索、编辑、删除
  - 文件目录进入、返回、新建、编辑、删除
  - 文件搜索、后台分片上传、进度展示、重命名、删除、解密下载
  - 好友通讯录、好友申请处理、好友详情与删除好友
  - 聊天主导航、会话列表、好友详情进入聊天、发起群聊、设备级端到端加密消息队列
  - 图片、语音、视频和文件类聊天附件先由客户端加密，再以服务端密文对象引用形式随聊天消息同步
  - 用户级加密聊天归档同步，供同账号多端解锁后恢复
  - 群聊列表从服务端恢复、当前用户群快照回写服务端
  - 手机前后台切换、锁屏恢复、网络恢复后的实时强制重连、待发消息补发与历史同步
  - 本地聊天回归冒烟脚本 `securex-app/tool/chat_realtime_smoke.dart`，当前覆盖好友在线状态、单聊/群聊在线直推、多设备同步、设备离线恢复、整用户离线恢复和群解散清理

## 生产发版与私有部署记忆

- 当前最近已发版本：`v1.0.25`，release commit 为 `917185d Release v1.0.25`
- `v1.0.25` 已推送 `main` 与 tag；GitHub Release workflow 由 tag 自动触发，当前环境未登录 GitHub CLI，无法直接查看 workflow 状态
- `v1.0.25` 主要修复 LiveKit 语音/视频通话稳定性：
  - 继承 `v1.0.24` 的服务端关键通话信令短 TTL 内存补投递、客户端通话信令重试、TURN relay 候选和视频主副窗口修复
  - 修复客户端每次发送通话信令前反复强制重连 WebSocket，导致端到端加密信令会话被主动打断并提示“接听信令发送失败”的问题
  - 放宽实时加密信令会话等待时间，降低移动网络和代理链路抖动下接听信令误判失败的概率
  - 私有 LiveKit 部署已调整为对客户端发布 TURN/TLS 标准 HTTPS 端口候选，匹配公网 SNI 分流入口
- 禁止把私有服务器别名、公网 IP、SSH 端口、真实部署目录、生产端口、备份文件名、生产配置内容写入 tracked 文件或 Git 提交
- 禁止把个人本机绝对路径写入 tracked 文件或 Git 提交，例如用户主目录、个人 workspace 路径、私有下载目录、临时排障路径等
- 面向团队协作的文档、脚本和示例默认使用仓库相对路径；确实需要说明安装目录时，使用可替换占位符或明确标注为部署示例，不绑定某个开发者机器
- 如需记录私有部署细节，只能写入 `.dev/private-deploy-notes.md` 这类已被 `.gitignore` 忽略的本地文件
- 更新私有后端部署的通用流程：
  - 本地先完成发版：更新 `CHANGELOG.md`，提交 release commit，运行 `./scripts/release-preflight.sh <tag>`，打 tag，先推 `main` 再推 tag
  - 如需立即更新服务器，可按 GitHub Release workflow 同参数本地构建 `linux-amd64` 后端包，或等待 Release 产物
  - 上传包到私有部署目录
  - 远端进入部署目录后先执行生产停止脚本
  - 替换前必须备份当前二进制
  - 解包后复制新 `secure-x` 与生产脚本，保留真实生产配置、数据、日志和运行态目录
  - 给新二进制和脚本补齐执行权限
  - 执行生产启动脚本、状态脚本和健康检查完成验证
- `v1.0.25` 无需替换私有后端二进制；`v1.0.24` 后端仍在私有后端运行，私有 LiveKit 配置已按 `v1.0.25` 记录调整；具体服务器和路径信息见本机忽略文件，不写入 Git

## 本地测试账号记忆

- 可用于本地联调的测试用户：
  - 用户名：`test`，登录密码：`12345678`，解锁密码：`yonyou@1988`
  - 用户名：`test2`，登录密码：`12345678`，解锁密码：`yonyou@1988`
  - 用户名：`test3`，邮箱：`test3@yonyou.com`，登录密码：`12345678`，解锁密码：`yonyou@1988`
- 这两个账号仅用于本地开发调试，尤其是好友和聊天双用户场景
- 测试凭据不要写入对外 README、示例配置或生产文档

## 文档维护规则

- `README.md`：对外说明项目目标、范围、架构摘要与文档入口
- `doc/`：沉淀详细设计、边界、路线图与后续决策
- `CHANGELOG.md`：每次改动必须更新
- 如果设计发生变化，优先修改 `doc/` 与 `README.md`
- 如果协作方式或长期约束变化，必须同步更新本文件

## 本仓库执行约束

- 终端命令优先使用 `rtk` 前缀
- 读取仓库现状后再动手，不凭空假设已有实现
- 不写网页端相关方案，除非用户后续明确追加
- 新增任何能力前必须先做安全边界检查：认证、授权、所有权、加密边界、明文暴露、日志泄露、临时文件、错误信息、权限绕过都要纳入考虑
- 设计优先考虑跨平台桌面与移动端复用
- 默认先做 `MVP`，再考虑团队分享、组织空间、自动填充等增强能力
- 写代码时优先复用成熟、流行、维护活跃的公共库，尽可能少写自研实现
- 除非公共库无法满足需求，或存在明确的安全、性能、许可、体积问题，否则不要动辄自行实现底层能力
- 对加密、认证、存储、状态管理、表单、网络请求等通用能力，优先做“可靠选型与合理封装”，而不是重复造轮子
- 前后端代码需要按功能、模块、页面或领域拆分，避免继续堆出千行大文件；新增功能优先落在清晰的小文件中，现有文件膨胀时要及时重构拆分
- Flutter 代码当前允许使用 `part` 按页面/控制器职责拆分同一 library 的私有实现，但拆分只能服务于可读性，不要把强相关逻辑拆得过碎
- Go 后端 HTTP 层按接口领域拆分 handler、request、helper，新增接口不要继续塞回单个 `router.go`
- 本地联调时，前后端启动与停止默认使用脚本入口，不要混用手工 `flutter run`、`go run`；兼容入口是 `./scripts/start-dev.sh` 和 `./scripts/stop-dev.sh`
- 我调试启动、停止、重启前后端或多前端实例时，必须和用户完全使用同一套项目脚本，不允许绕过脚本直接运行底层启动命令
- 我需要联调时必须先检查用户是否已经通过脚本启动了可用实例；如果已有可用后端或前端实例，优先复用用户当前实例，不随意再启动新进程
- 只有实例缺失、健康检查失败、进程异常，或明确需要额外多用户隔离前端实例时，才通过同一套脚本启动新的实例
- 本地联调默认只操作仓库脚本启动的调试实例，不要影响用户系统中已安装的 `secure-x` 安装版应用；除非用户明确要求排查安装版，否则不要登录、关闭、点击或向安装版注入测试数据
- 后端单独启动/停止使用 `./scripts/start-dev-be.sh` 与 `./scripts/stop-dev-be.sh`
- 前端单独启动/停止使用 `./scripts/start-dev-app.sh` 与 `./scripts/stop-dev-app.sh`
- 同时启动/停止使用 `./scripts/start-dev-all.sh` 与 `./scripts/stop-dev-all.sh`
- 后端运行参数统一使用 YAML 配置文件，不再使用 `SECUREX_*` 环境变量；开发脚本生成 `.dev/securex-be.yaml` 并通过 `--config` 传给后端，生产真实配置文件不得提交到仓库
- 除非用户明确要求排查底层启动命令，否则不要直接混用 `flutter run`、`go run` 与脚本方式
- `start-dev-app.sh` 允许重复运行以启动多个前端实例，便于调试两个用户聊天；需要稳定复用某个用户实例时传 `APP_INSTANCE=user-a`
- 同一个 `APP_INSTANCE` 已运行时脚本不能再启动第二个同名实例，避免同一本地命名空间被两个进程同时使用
- 多前端实例必须使用独立本地存储命名空间，禁止共享 token、SharedPreferences、聊天本机密文队列或其他用户本地数据
- 当前 Flutter 客户端会按 `SECUREX_DEV_INSTANCE` 隔离本地存储 key 和聊天队列文件；脚本会基于 `APP_INSTANCE` 注入该变量
- 需要重启、切换设备或怀疑有残留进程时，优先通过对应脚本停掉旧实例后再启动；后端保持单实例，前端可多实例
- 我通过脚本重启本地测试前端实例后，如果实例对应已记录测试用户（如 `APP_INSTANCE=test`、`test2`、`test3`）且用户已提供登录密码与解锁密码，应尽量继续帮用户登录并解锁到主界面，方便用户直接测试；仅限本地开发测试账号，不适用于生产账号或未授权账号
- 聊天链路的本地回归优先使用 `rtk dart run tool/chat_realtime_smoke.dart`，先在仓库调试环境把好友在线状态、单聊、群聊、多设备和离线恢复跑通，再决定是否需要额外手工联调
- 前端涉及 `创建 / 编辑 / 详情` 的子流程时，默认优先使用独立子页面承载，并提供顶部返回按钮返回上一层，而不是使用 `AlertDialog` 一类悬浮弹窗
- 未来新增子页面默认遵循“进入子页面 -> 顶部返回 -> 返回上一页”的统一交互；如果现有页面不符合，后续迭代时优先往这个方向收敛
- 前端颜色风格统一从 `SecureXThemeSpec` 与 `SecureXPalette` 主题变量中获取，新增页面不要随意散落硬编码颜色；如果确需新增颜色，应先扩展主题变量
- 设置页应保留主题切换能力，至少覆盖日间与夜间主题，确保主要组件在浅色/深色主题下都有足够对比度
- 设置或子页面里触发 `锁定`、`退出登录` 等会改变认证/解锁状态的操作时，必须主动退出子页面导航栈，让用户立即看到锁定页或登录页，不要依赖用户手动点返回
- 密码库的“分类”只用于组织密码/登录项；文件模块不复用密码分类，必须按网盘目录语义独立建模与展示
- 密码库需要保留独立的“分类管理”入口，分类支持新增、修改、删除；删除前必须先判断分类下是否还有密码项或子分类，有则提示用户先处理
- 文件模块中的目录名、文件名、文件密钥等业务数据仍必须由客户端加密后提交；服务端只保存密文目录元数据与密文文件
- 后端保存或更新带有关联 ID 的资源时，必须校验该关联资源属于当前登录用户；禁止跨用户引用造成脏数据或潜在越权链路
- 好友功能只能管理社交关系和公开账号信息，不能复用好友关系作为密码库、文件库或密文资源的访问授权；未来如果增加共享，必须单独设计显式授权与客户端密钥共享协议
- 聊天功能默认采用“`WebSocket + 应用层端到端加密`”主链路：客户端按目标用户设备逐个加密消息，服务端只保存不可解密的密文信封并负责在线通知/待拉取，同时允许保存“每个用户自己的加密聊天归档”作为主同步存储，但不得保存可解密正文或会话密钥
- 当前图文聊天实现以 `ChatProtocol` 抽象协议层，默认实现为 `securex-e2ee-v1`：每设备一个长期身份种子与公钥目录，发送时按接收设备逐个做 ECDH + AES-GCM 密封，方便后续替换更强协议而不把实现写死
- 普通图文聊天不再默认使用 WebRTC；WebRTC 类能力后续优先只用于语音、视频、屏幕共享或大文件直连等实时媒体场景
- 聊天实时通道应在登录解锁后自动连接，包含心跳与断线重连；正常聊天界面不应要求用户手动点击“连接”才能收发消息
- 聊天语音消息属于隐私敏感能力：默认只在用户主动点击后录制，并作为端到端加密附件发送；不做默认语音转文字，不上传可解密音频到 secure-x 服务端
- 聊天短视频附件也属于敏感聊天内容，必须按端到端加密附件或加密文件引用处理；直接内嵌聊天归档的视频必须限制大小，避免拖垮实时队列、客户端内存和服务端密文归档
- 实时语音/视频通话默认采用自托管 `LiveKit` 承载媒体链路，secure-x 后端只负责配置下发、好友/共同群成员权限校验和短期 LiveKit 房间 token 签发；客户端不得硬编码 LiveKit、TURN、信令端口或域名
- LiveKit 可采用内置 TURN/TLS 与公网 `443/TCP` 复用的最简部署模式；这能显著减少端口暴露和移动网络 NAT 问题，但媒体仍需按最小权限、弱网重连、通话状态同步和未来媒体级 E2EE 升级空间设计
- 聊天消息可靠性是硬约束：不能因为实时通知丢失、设备过期、弱网重试、客户端重启或服务端清理造成硬丢消息；服务端待拉取密文信封只有在目标设备成功解密并落入本地会话后才能确认删除
- 聊天送达体验目标是正常在线或前台弱网场景下 `10 秒内可感知`；超过 10 秒仍未收到要优先排查待拉取队列、设备目录膨胀、反向代理 websocket、客户端轮询和确认链路，而不是让用户手动下拉刷新兜底
- 批量聊天投递不能因为单个过期设备或无效设备导致整批合法设备入队失败；非法目标应跳过并记录最小诊断日志，合法目标继续保存不可解密密文信封
- 设计聊天与实时能力时必须考虑手机移动网络现实情况：应用前后台切换、Wi‑Fi/蜂窝切换、弱网抖动、运营商 NAT 与长连接超时都要纳入恢复策略；不能假设 WebRTC P2P 在手机网络下一定稳定可用
- 当移动网络环境下直连能力不稳定时，应优先保证“端到端加密 + 服务端只接触不可解密密文 + 协议可升级”的安全边界，再通过 WebSocket 在线密文中继与待拉取队列兜底可用性
- 聊天图片、语音、视频和文件附件默认也属于敏感聊天内容，必须先由客户端加密，再以“服务端不可解密密文对象 + 聊天消息内端到端加密引用”的方式同步；服务端只能按所有者和允许接收用户做下载授权，不能保存可解密附件内容
- 手机锁屏、切后台或网络中断后，客户端重新回到前台或恢复联网时，必须主动强制重建实时连接，并自动尝试补发尚未同步到服务端的密文消息、拉取待处理密文信封，并与在线好友或共同群成员做增量历史对账
- 登录解锁后应优先从服务端加载“当前用户自己的加密聊天归档”恢复聊天列表，本机缓存只作为性能缓存与故障兜底；随后再向在线好友或共同群成员发起端到端历史同步，请求和响应只能走客户端实时加密通道
- 群聊消息和单聊一样不得以可解密形式存服务端；当前群聊按群成员设备逐个端到端加密投递，服务端只保存群元数据、每用户自己的加密归档和短期待拉取密文信封
- 群聊“有哪些群、有哪些成员、谁是群管理”这类最小必要元数据可以存服务端，但只能用于路由、授权、多端恢复和在线状态判断；服务端仍不得保存可解密聊天正文
- 如果服务端确实需要保存聊天相关快照，只能保存“每个用户各自的端到端加密聊天/群聊归档”，且这些归档默认不包含服务端可解密明文；服务端、其他用户、管理员都不能解密
- 单聊或群聊为了多端同步而上云时，优先采用“每个用户自己的端到端加密聊天归档”作为主存储方案，本机文件降级为缓存与离线兜底，不要默认设计成服务端可读取的全量离线消息库
- 群聊第一个创建者是群管理，可在本机群信息中移除成员；其他成员可邀请自己的好友进群，后续消息只发送给当前本机群成员列表
- 群设置必须提供退出群能力；退出前需要确认，退出后必须删除当前账号在本机缓存和服务端加密归档中的该群会话、成员元数据和全部聊天记录；如果群管理退出，按当前成员顺序自动把下一位成员升级为群管理，并尽量通过端到端实时通道通知在线成员
- 群管理解散群后，当前群管理自己的本地会话、服务端加密归档和群成员关系应立即删除；其他成员保留一个“已解散”的只读会话占位，只能删除自己的当前账号会话，随后再逐步清理剩余群元数据
- 群成员变更不能降低安全要求；后续实现群历史同步、成员删除后的密钥轮换、上线向在线成员拉取消息时，必须设计显式群密钥协议，不能把可解密消息托管给服务端
- 聊天相关的客户端本地结构、服务端表结构和密文载荷都必须带版本或协议标识；升级只能做增量迁移，不能因为应用升级、配置变更或协议切换导致用户本地聊天数据、服务端密文队列、群元数据或聊天归档丢失
- 设备身份、公钥目录、用户级加密聊天归档和待拉取密文信封属于聊天同步基础设施；后续改协议时优先在兼容层或迁移层处理，不直接破坏既有数据
- 聊天设备身份不能依赖硬件指纹或系统不可控设备号；客户端应生成随机 `deviceId` 与设备密钥种子，并同时保存到系统安全存储、本地保险库密钥加密兜底文件，以及服务端不可解密的设备身份恢复包；卸载重装后必须在用户登录并输入解锁密码后优先恢复同一个 Secure X 设备身份
- 服务端必须对聊天设备注册做防爆保护，包括新增设备速率限制、历史设备裁剪和活跃设备投递上限，避免异常客户端或安全存储失效导致单用户堆出海量设备记录，进而拖垮单聊/群聊投递
- 设置页需要保留设备管理入口，允许用户查看当前账号聊天设备、最近在线时间并删除旧设备；删除设备不得暴露或影响保险库明文，只能清理该设备的投递路由和待拉取密文
- 聊天归档后续允许从“整包密文快照”逐步升级到“按会话分段的加密归档清单 + 会话详情”模型，但升级期必须保留旧格式兼容读取、平滑迁移和本地缓存兜底，禁止因为归档格式重构导致旧消息丢失
- 如果客户端需要 WebSocket、LiveKit、TURN/TLS、WebRTC 信令或 ICE 服务配置，必须先通过后端 HTTP 接口获取服务器实时服务信息，不能在客户端写死端口、域名或协议
- 跨平台本地聊天存储要使用 Flutter 跨平台目录能力，当前优先使用应用支持目录保存加密后的聊天缓存；缓存必须可被服务端加密归档重新恢复，不能成为唯一历史来源
- 大文件上传不能阻塞主界面；客户端应优先使用后台上传任务、进度展示与分片上传协议。当前已支持应用内后台分片上传和服务端分片合并，后续再补“应用重启后自动恢复未完成上传”
- 后端 JSON 接口必须统一返回以下结构：
  - `success`：接口处理是否成功
  - `message`：用于前端直接展示的说明信息
  - `httpCode`：与真实 HTTP 状态一致
  - `data`：具体业务数据，没有数据时返回空对象
- 除非是未来明确约定的特殊流式协议，否则不要再返回裸 JSON、裸字符串错误或空响应体
- 面向用户展示的错误提示必须使用中文，尤其是登录、注册、解锁、网络连接、Token 失效等常见失败场景；前端不要直接展示 Dio、cryptography、平台异常等库的英文原始错误
- 用户侧统一称呼为 `解锁密码`，不要再在界面和面向用户的文档里使用“主密码”；登录时只输入账号登录密码，登录成功后再进入解锁页输入解锁密码
- 设置页中的密码相关入口统一放到 `安全设置` 下，包含 `登录密码` 和 `解锁密码`；修改解锁密码只能在客户端校验并重新封装保险库密钥，服务端只保存新的密文封装配置
- GitHub Release 打包由 `.github/workflows/release.yml` 维护，推送 `v*` tag 后构建前端 Windows/macOS/Android/iOS 与后端 Linux/macOS 产物，并从 `CHANGELOG.md` 中与当前 tag 匹配的版本章节生成 Release 内容
- Flutter 发版构建时必须把目标 tag 的语义版本注入应用内部版本：`build-name` 必须与当前 `v*` tag 对齐，`build-number` 必须保持单调递增；不能只改安装包文件名而让应用内显示版本停留在旧值
- `CHANGELOG.md` 发布章节必须同时包含版本与日期，格式为 `## [vX.Y.Z] - YYYY-MM-DD`；未发布变更可先放在 `## [Unreleased]`
- 除非用户明确说“发版”、指定要发布某个版本，或明确要求推送 release tag，否则不要主动执行发版流程；修复、排查和部署配置调整默认只做到代码修改、验证和说明，不能擅自提交 release commit、创建 `v*` tag、推送 tag 或触发 GitHub Release workflow
- 用户说“发版”时，默认从当前最新 `v*` tag 自动递增 patch 版本；例如当前最新为 `v1.0.0`，下一次就是 `v1.0.1`，除非用户明确指定其他版本
- 发版前必须先整理代码和文档，更新对应版本的 `CHANGELOG.md` 内容，然后提交、打 tag、推送 `main` 与 tag；不要跳过文档和 changelog
- 发版时必须先确保 release commit 已提交到当前 `HEAD`，再打对应版本 tag；禁止先 push tag 再补 release commit，也禁止把已存在 tag 改指向新提交
- 发版前默认先运行 `./scripts/release-preflight.sh <tag>` 或 `./scripts/release-preflight.sh --next` 做校验，确认工作区干净、`CHANGELOG.md` 章节存在、目标 tag 未占用且当前 `HEAD` 已包含该版本 changelog
- 推送发版时优先显式分两步执行：先 `git push origin main`，再 `git push origin <tag>`；不要把未来 tag 提前推到旧提交上
- GitHub Actions 页面手工运行 `Release` workflow 时，只能针对已经存在且指向正确提交的 tag 补跑，不能拿尚未创建或指向错误提交的版本号直接手工发版
- 发版前必须更新 `CHANGELOG.md`；生产证书、签名文件、后端真实配置和密钥材料禁止提交到仓库，后续正式签名应使用 GitHub Secrets 或私有签名服务
- 后端 release 包内可执行文件固定命名为 `secure-x`，前端各平台应用显示名也统一为 `secure-x`
- 后端生产包应包含 `start.sh`、`stop.sh`、`status.sh` 与 `secure-x.service` 参考文件，生产日志默认支持应用日志与访问日志输出目录配置
- 前端新增跨平台能力时必须同步检查平台权限与安全策略：Android Manifest、iOS Info.plist、macOS entitlements、Windows/Linux 平台配置都要按最小权限原则补齐；移动端生产后端地址默认建议使用 HTTPS，明文 HTTP 只为本机或局域网调试保留明确例外
- 个人头像属于公开资料，不按保险库敏感数据处理；允许上传本地图片时必须限制文件类型、文件大小、存储路径和展示范围，不能把头像上传通道做成任意文件上传入口
- 应用内版本更新从 `ligson/secure-x` GitHub Release 获取最新版本；Android/桌面端可下载并打开匹配平台的安装包，iOS 不能应用内自安装，只能跳转 Release/TestFlight/App Store 等系统允许渠道

## 当前文档清单

- [README.md](README.md)
- [doc/README.md](doc/README.md)
- [doc/01-product-overview.md](doc/01-product-overview.md)
- [doc/02-architecture.md](doc/02-architecture.md)
- [doc/03-crypto-storage.md](doc/03-crypto-storage.md)
- [doc/04-roadmap.md](doc/04-roadmap.md)
- [doc/05-open-questions.md](doc/05-open-questions.md)
- [doc/06-release.md](doc/06-release.md)
- [CHANGELOG.md](CHANGELOG.md)

## 待确认事项

- 注册是否采用邮箱验证码，还是只做用户名 + 解锁密码
- 是否支持找回账号，但不支持找回解锁密码
- 初期是否只做单用户个人空间，不做共享保险库
- 文件大小上限、同步冲突策略、离线缓存策略的最终取舍
